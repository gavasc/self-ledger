name: self-ledger
arch: amd64
platform: linux
version: ${VERSION}
maintainer: gv
description: Local-first personal finance desktop app. No cloud, no accounts — all data in SQLite on your machine.
contents:
  - src: build/bin/self-ledger
    dst: /usr/local/bin/self-ledger
overrides:
  deb:
    dependencies:
      - libwebkit2gtk-4.0-0
      - libgtk-3-0
  rpm:
    dependencies:
      - webkit2gtk4.0
      - gtk3
